<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.carpoolbuddy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarpoolBuddy">
        <activity
            android:name=".Model.AuthActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Model.UserProfileActivity"
            android:exported="false" />
        <activity
            android:name=".vehicles_row_view"
            android:exported="false" />
        <activity
            android:name=".Model.CreateUserActivity"
            android:exported="false" />
        <activity
            android:name=".Model.VehicleProfileActivity"
            android:exported="false" />
        <activity
            android:name=".Model.VehiclesInfoActivity"
            android:exported="false" />
        <activity
            android:name=".Model.AddVehicleActivity"
            android:exported="false" />
    </application>

</manifest>